1. Extract all the sentences from the job adverts and generate embeddings for these sentences

2. Apply the JQ classifier to the embeddings to predict whether the sentence relates to job quality. Any sentence with probability >= 0.3 is considered to relate to job quality.
